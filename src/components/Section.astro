---
interface Props {
  className?: string;
  id?: string;
}

const { className = "", id } = Astro.props;
---

<section id={id} class={`max-w-4xl mx-auto px-6 md:px-8 py-20 md:py-32 ${className}`}>
  <slot />
</section>
